pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "MaxRTB"

include ':core'
include ':zx'
include ':zxdm'

// 可选：根据需要选择include的Provider
// include ':csj'
// include ':gdt'
// include ':ks'
// include ':baidu'
// include ':mg'
